# iBreeze 需求规格说明书

> 本文档面向**无代码经验的读者**，目标是让读者能够依据本文与指定技术栈，**从零重建整个 iBreeze 项目**，并且文档内容与代码逐一对齐。
> 文档中的所有接口、表结构、状态机、业务规则均来自 `sidecar/`、`apps/desktop/`、`admin-backend/` 与 `docs/` 下的实际实现，可据此逐条核对。

---

## 第 1 章 概述与目标

### 1.1 产品定位

iBreeze 是一个**多租户 AI 协作后端平台**。它把"组织 / 权限 / 能力 / 后端 Provider / 会话 / 任务 / 知识库 / 治理"统一编排在一个可嵌入桌面端的 sidecar 进程中，对外暴露一套统一的 JSON-RPC 接口。

- **后端进程（sidecar）**：`sidecar/` —— 一个常驻的 async Python 服务，监听 Unix Domain Socket（UDS），提供约 **71 个**活跃 RPC 方法（会话、任务、工作流、运行时调用等）。
- **桌面前端（Tauri + React）**：`apps/desktop/` —— Tauri 2 外壳内嵌 React 19 单页应用，通过 Tauri `invoke` 调用 Rust 桥接，再由 Rust 转发到 sidecar 的 UDS。瘦身后保留 **9 个**页面（公司、员工、会话、任务、任务高级、仪表盘、设置 + 详情页）。
- **Admin Backend（REST API）**：`admin-backend/` —— 基于 FastAPI 的独立管理后端，监听 `http://127.0.0.1:50080`，提供约 **77 个** REST 端点，覆盖能力/技能/提示词/模板/知识库/Provider/后端/治理/审计/同步等管理 CRUD 操作。
- **Admin Frontend（Web 管理面板）**：`admin-frontend/` —— 基于 React 18 + Ant Design 5 + ProComponents 的独立管理前端，监听 `http://localhost:50081`，提供 **11 个**管理页面。

### 1.2 技术栈

| 层 | 技术 |
|----|------|
| Sidecar 后端语言 | Python 3.11+，全程 async（asyncio） |
| Sidecar 关系存储 | SQLite（`aiosqlite` 异步驱动），运行在 `/tmp/acos.db` |
| Sidecar 向量 / 文档存储 | LanceDB（sidecar 内嵌，用于知识库、文档、citation 索引） |
| Sidecar 进程间通信 | Unix Domain Socket（`/tmp/acos.sock`）+ JSON-RPC 2.0 风格自研协议 |
| 桌面外壳 | Tauri 2（Rust） |
| 桌面前端框架 | React 19 |
| 桌面前端状态 | Zustand（全局 store）+ TanStack Query（服务端缓存/请求） |
| 桌面包管理 / 构建 | npm（前端）、cargo（Rust）、tauri CLI |
| Admin Backend 后端语言 | Python 3.12+ / FastAPI / SQLAlchemy + aiosqlite / Alembic |
| Admin Backend 存储 | SQLite（`/tmp/ibreeze_admin.db`） |
| Admin Backend 认证 | JWT（HS256） + RBAC（角色：admin/editor/viewer） |
| Admin Frontend 前端框架 | React 18 + TypeScript + Vite + Ant Design 5 + ProComponents |

### 1.3 应用标识

- `app` 标识：`com.ibreeze.desktop`
- `productName`：`iBreeze`

### 1.4 设计目标

1. **强一致多租户**：所有数据归属 `company_id`，跨公司访问一律拒绝。
2. **可审计**：关键写操作进入审计表与领域事件表。
3. **可幂等**：所有"写方法"必须携带 `idempotency_key`，服务端统一预约并回填，崩溃重试不重复生效。
4. **可治理**：预算、审批、权限引擎统一收敛到治理层。
5. **可重建**：所有表结构由 `sidecar/migrations/` 顺序迁移生成，文档第 4 章与迁移文件一一对应。
6. **管理分离**：能力/技能/提示词/模板/Provider/后端/治理/审计等管理操作已从 Sidecar 迁移到 Admin Backend REST API，Sidecar 仅保留运行时必需方法。

### 1.5 关键非功能性约定

- **前端时间显示**：所有时间统一换算到**东八区（Asia/Shanghai）**展示，禁止浏览器本地时区偏移。带 `Z` 或 offset 的先换算，无时区的后端字符串按北京时间墙钟展示。
- **前端数值显示**：所有数值经统一过滤器处理，**最多保留 2 位小数且不补零**。
- **错误码**：RPC 失败统一通过 `error` 字段返回，含 `code` 与 `message`（详见 3.4 / 各域契约）。

---

## 第 2 章 领域与边界

### 2.1 领域划分

| 域（命名空间前缀） | 含义 | 归属 |
|-------------------|------|------|
| `org` | 组织、部门、员工、模板、授权、权限、组织图 | Sidecar（运行时）+ Admin Backend（管理 CRUD） |
| `cap` | 能力（capability）、技能（skill）、提示词（prompt）、版本、快照、指标、解析引擎 | Sidecar 保留 2 个运行时方法；其余管理操作在 Admin Backend |
| `backend` | AI 后端实例（启用/排水/禁用/归档、探活、默认后端） | Sidecar 保留运行时方法；管理 CRUD 在 Admin Backend |
| `provider` | Provider 厂商、模型、价格、凭证、预算冻结、可用性、运行时 | Sidecar 保留 5 个运行时方法；管理 CRUD 在 Admin Backend |
| `session` | 会话、消息、转交、暂停、对账、转录 | Sidecar（全部运行时方法） |
| `task` | 任务、子任务节点、启动/取消/完成/重试 | Sidecar（全部运行时方法） |
| `workflow` | 工作流 plan 校验、checkpoint、deadletter、task 取消 | Sidecar（全部运行时方法） |
| `knowledge` | 知识库文档 CRUD、检索、更新、删除 | Sidecar 保留检索；管理 CRUD 在 Admin Backend |
| `kg` | 知识图谱：文档、来源、citation、搜索、摄取重试、确认/驳回、重索引 | Sidecar（运行时方法）+ Admin Backend（管理操作） |
| `gov` | 治理：预算策略、预算、审批类型、审计 | Sidecar 保留运行时审批；管理操作在 Admin Backend |
| `approval` | 审批请求/决议 | Sidecar（运行时方法） |
| `audit` | 审计查询、人工干预 | Admin Backend（REST API） |
| `settings` | 公司级/知识库/通知/安全/工作区策略设置 | Admin Backend（REST API 同步） |
| `sys` | 系统：健康、关闭、迁移状态 | Sidecar（内置桩） |
| `echo` | 自测回显（8 个类型） | Sidecar（自测桩） |

### 2.2 绑定链与归属

- **Provider ← Template ← Employee**：员工使用模板，模板绑定 Provider；三者全部归属某个 `Company`。
- 所有跨域查询必须以 `company_id` 为首要过滤条件。
- **软删除**：`company`、`employee` 等核心实体采用 `deleted_at` 软删除，物理删除仅在解散（dissolve）收尾阶段或显式 archive 流程中触发。

### 2.3 进程边界（谁调谁）

```
桌面端：
React 19 (Zustand + TanStack Query)
   │  invoke('sys_rpc_call', { method, params: JSON.stringify(params) })
   ▼
Tauri Rust 桥 (apps/desktop/src-tauri)
   │  UDS 写 /tmp/acos.sock
   ▼
sidecar RPCServer (async Python)
   ├─ 幂等预约 / 校验 / 分发 (_dispatch)
   ├─ 各 Methods 集合（session/task/workflow/kg.org 运行时方法）
   ├─ 治理层 (gov): 预算 / 审批 / 权限引擎
   ├─ SQLite (/tmp/acos.db)  ── aiosqlite
   └─ LanceDB                 ── 向量/文档

管理端：
Admin Frontend (React 18 + Ant Design 5)
   │  HTTP REST 调用
   ▼
Admin Backend (FastAPI)
   ├─ JWT 认证 / RBAC 权限
   ├─ 能力/技能/提示词/模板 CRUD
   ├─ Provider/后端管理
   ├─ 治理/审计/同步
   ├─ SQLite (/tmp/ibreeze_admin.db)  ── aiosqlite
   └─ REST Pull ← Sidecar（数据同步）

数据同步：
Sidecar ──(REST Pull)──→ Admin Backend
   管理配置从 Admin Backend 同步到 Sidecar
```

### 2.4 接口调用约定（通用）

**Sidecar RPC 调用**（桌面端使用）：

```ts
invoke('sys_rpc_call', { method: 'org.company.list', params: JSON.stringify({ ... }) })
```

- `method`：字符串，形如 `org.company.list`（见第 3 章完整清单，共约 **62** 个活跃方法）。
- `params`：**JSON 字符串**（不是对象），服务端反序列化。
- 写方法必须额外携带 `idempotency_key`（字符串）。服务端在 `_dispatch` 中：
  1. 若方法是写方法且带 `idempotency_key` → 先预约幂等记录（保留成功/失败结果）；
  2. 执行 handler；
  3. 通过 `complete_idempotency(...)` 回填结果。
  4. 同一 `idempotency_key` 重复提交 → 直接返回首次结果，不二次执行。

**Admin Backend REST 调用**（管理端使用）：

```bash
curl -X GET http://127.0.0.1:50080/api/companies \
  -H "Authorization: Bearer <jwt_token>"
```

- 所有管理端点均需 JWT 认证（`Authorization: Bearer <token>`）。
- 写操作需 `admin` 或 `editor` 角色。
- 分页参数：`skip`（偏移量）、`limit`（每页数量，默认 50）。

---

## 第 3 章 RPC 接口契约

> 本章按域分组列出全部 Sidecar RPC 方法。**瘦身后 Sidecar 保留约 71 个活跃方法**，原管理类方法（capability/skill/prompt/template/backend/provider 管理、gov/approval/audit/settings 的管理操作）已迁移到 Admin Backend REST API（见 3.X 节）。
> 由于方法数量庞大，下文按"域 → 方法表"呈现；读者可与 `sidecar/acos/rpc/methods_*.py` 逐条对照。

### 3.0 系统内置 / 自测

| method | 说明 | 入参 | 出参 |
|--------|------|------|------|
| `sys.health` | 健康检查（server 内置桩） | `{}` | `{ ok: true, ts }` |
| `sys.shutdown` | 优雅关闭（server 内置桩） | `{}` | `{ ok: true }` |
| `sys.migration.status` | 迁移状态 | `{}` | 已应用迁移列表/版本 |
| `echo.string` | 回显字符串 | `{ value }` | `{ value }` |
| `echo.int` | 回显整数 | `{ value }` | `{ value }` |
| `echo.float` | 回显浮点 | `{ value }` | `{ value }` |
| `echo.bool` | 回显布尔 | `{ value }` | `{ value }` |
| `echo.array` | 回显数组 | `{ value }` | `{ value }` |
| `echo.object` | 回显对象 | `{ value }` | `{ value }` |
| `echo.null` | 回显 null | `{}` | `{ value: null }` |
| `echo.mirror` | 原样镜像 | `{ ... }` | 原 params |

### 3.1 org —— 组织域（35 个运行时方法）

#### 3.1.1 company

| method | 说明 | 关键入参 | 关键规则 / 错误 |
|--------|------|----------|----------------|
| `org.company.list` | 列出公司 | `filter?` | 仅返回当前上下文可见 |
| `org.company.get` | 获取公司 | `{ id }` | 不存在 → error |
| `org.company.create` | 创建公司 | `{ name, ... }` | 初始状态 `initializing`；写方法需 `idempotency_key` |
| `org.company.update` | 更新公司 | `{ id, patch }` | |
| `org.company.delete` | 软删除 | `{ id }` | 置 `deleted_at`；需无活跃依赖 |
| `org.company.restore` | 恢复 | `{ id }` | 清除 `deleted_at` |
| `org.company.activate` | 激活 | `{ id }` | `initializing → active` |
| `org.company.dissolve` | 解散 | `{ id }` | 进入 `dissolving`，触发 6 消费者 `dissolution_watermarks` 流水线 |

#### 3.1.2 department

| method | 说明 | 关键规则 |
|--------|------|----------|
| `org.department.list` | 列表 | |
| `org.department.get` | 获取 | |
| `org.department.create` | 创建 | 父部门冻结 → 禁止创建子部门 |
| `org.department.update` | 更新 | |
| `org.department.delete` | 删除 | 含员工 / 含子部门 → 拒绝 |
| `org.department.move` | 移动 | |
| `org.department.setLeader` | 设主管 | |
| `org.department.freeze` | 冻结 | `active → frozen` |
| `org.department.unfreeze` | 解冻 | `frozen → active` |
| `org.department.archive` | 归档 | `active/frozen → archived` |

#### 3.1.3 employee

| method | 说明 | 关键规则 |
|--------|------|----------|
| `org.employee.list` | 列表 | |
| `org.employee.get` | 获取 | |
| `org.employee.create` | 创建 | 初始 `created`，经激活转 `active` |
| `org.employee.update` | 更新 | |
| `org.employee.delete` | 删除 | 软删（`deleted_at`） |
| `org.employee.activate` | 激活 | `created → active` |
| `org.employee.suspend` | 暂停 | `active → suspended` |
| `org.employee.resume` | 恢复 | `suspended → active` |
| `org.employee.archive` | 归档 | `→ archived` |
| `org.employee.transfer` | 调动 | 跨部门/公司边界校验 |
| `org.employee.setManager` | 设上级 | |

#### 3.1.4 template（已迁移到 Admin Backend REST API）

> **注意**：template 的管理操作（list/get/create/update/activate/archive）已迁移到 Admin Backend REST API（见 3.15.6 节）。以下方法在 Sidecar 中仍注册但仅用于内部调用，不作为对外活跃方法计数。

| method | 说明 | 关键规则 |
|--------|------|----------|
| `org.template.list` | 列表 | 已迁移到 Admin Backend |
| `org.template.get` | 获取 | 已迁移到 Admin Backend |
| `org.template.create` | 创建 | 已迁移到 Admin Backend |
| `org.template.update` | 更新 | 已迁移到 Admin Backend |
| `org.template.activate` | 激活 | 已迁移到 Admin Backend |
| `org.template.archive` | 归档 | 已迁移到 Admin Backend |

#### 3.1.5 grant

| method | 说明 |
|--------|------|
| `org.grant.create` | 创建授权 |
| `org.grant.get` | 获取 |
| `org.grant.list` | 列表 |
| `org.grant.revoke` | 撤销（置 `revoked_at`） |

#### 3.1.6 其他 org（org.permission / org.graph）

| method | 说明 |
|--------|------|
| `org.permission.resolve` | 权限引擎解析（委托 `cap.engine.resolve`） |
| `org.graph.get` | 组织图（部门/员工树） |

### 3.2 cap —— 能力域（Sidecar 保留运行时方法）

> **注意**：capability/skill/prompt 的完整 CRUD（create/get/list/update/archive/deprecate/publish/submitReview/createVersion）已迁移到 Admin Backend REST API。Sidecar 仅保留运行时解析和快照构建方法。

| method | 说明 |
|--------|------|
| `cap.snapshot.build` | 构建能力快照 |
| `cap.engine.resolve` | 权限/能力解析引擎（被 `org.permission.resolve` 调用） |

### 3.3 backend —— 后端域（Sidecar 保留运行时方法）

> **注意**：backend 的管理操作（create/get/list/update/enable/drain/archive/setDefault）已迁移到 Admin Backend REST API。Sidecar 仅保留探活方法。

| method | 说明 |
|--------|------|
| `backend.checkAvailability` | 检查后端可用性 |
| `backend.probe` | 探活 |

### 3.4 provider —— Provider 域（Sidecar 保留运行时方法）

> **注意**：provider 的管理操作（create/list/credential.set/get/revoke/pricingPolicy/tierMapping/budgetFreeze）已迁移到 Admin Backend REST API。Sidecar 仅保留运行时调用方法。

| method | 说明 | 备注 |
|--------|------|------|
| `provider.list` | 列出 Provider | |
| `provider.model.list` | 模型列表 | |
| `provider.runtime.start` | 启动运行时 | `provider.runtime.*` |
| `provider.runtime.send` | 发送请求 | |
| `provider.runtime.cancel` | 取消运行时 | |

> 说明：`provider.*` 在 Sidecar 中保留 5 个运行时方法。

### 3.5 session —— 会话域（10）

`session.list / get / sendMessage / cancel / resume / transcript.get / _suspend / _handoff / _reconcile / _archive`

- 生命周期：`active → running → waiting_backend → waiting_approval → dormant → archived / failed / recovering`（见第 5 章）。
- 触发 `READONLY` 的条件：会话所属员工 `suspended`，或所用模板 `archived`。
- `_suspend / _handoff / _reconcile / _archive` 为内部维护方法（下划线前缀）。

### 3.6 task —— 任务域（6）

`task.create / start / complete / cancel / retrySubtask / nodes`

- 关联 `task_node`（子任务节点）、`task` 主表、`plan_generations`、`checkpoints`。
- `retrySubtask`：重跑失败子节点；`nodes`：返回节点树。

### 3.7 workflow —— 工作流域（4）

| method | 说明 |
|--------|------|
| `workflow.plan.validate` | 校验 plan（委托 `task/plan_validator.py`，必要时创建审批） |
| `workflow.checkpoint.list` | 列出 checkpoint |
| `workflow.deadletter.resolve` | 处理死信（与 task 联动） |
| `workflow.task.cancel` | 取消工作流任务 |

### 3.8 knowledge —— 知识库域（Sidecar 保留运行时方法）

> **注意**：knowledge 的管理 CRUD 已迁移到 Admin Backend REST API。Sidecar 保留运行时检索方法。

| method | 说明 |
|--------|------|
| `knowledge.search` | 向量检索（走 LanceDB） |

### 3.9 kg —— 知识图谱域（4）

`kg.document.list / get / citation.get / search`

- 管理操作（source.delete / ingest.retry / knowledge.confirm / reject / reindex）已迁移到 Admin Backend REST API。

### 3.10 gov —— 治理域（Sidecar 保留运行时方法）

> **注意**：gov 的管理操作（budgetPolicy/budget/approvalType/audit）已迁移到 Admin Backend REST API。Sidecar 保留运行时审批方法。

| method | 说明 |
|--------|------|
| `gov.budget.get` | 获取预算 |
| `gov.budget.reserve` | 预留预算 |
| `gov.budget.revise` | 修订预算 |

### 3.11 approval —— 审批域（4）

`approval.request / list / get / resolve`

- 与 `gov.approvalType.*` 协同；`resolve` 决议通过/驳回。

### 3.12 audit —— 审计域（管理操作在 Admin Backend）

> **注意**：audit 的查询操作已迁移到 Admin Backend REST API。Sidecar 不再直接暴露 audit 方法。

### 3.13 settings —— 设置域（管理操作在 Admin Backend）

> **注意**：settings 的管理操作已迁移到 Admin Backend REST API（通过 sync 端点同步到 Sidecar）。

### 3.14 方法总数核对

#### Sidecar 注册方法（102 个）

| 域 | 注册方法数 | 说明 |
|----|-----------|------|
| echo | 8 | string, int, float, bool, array, object, null, mirror |
| sys | 4 | health, shutdown, migration.status, sync.trigger |
| org | 35 | company 8 + department 10 + employee 11 + grant 4 + graph 1 + permission 1 |
| cap | 2 | snapshot.build + engine.resolve |
| backend | 10 | list, get, create, update, setDefault, enable, drain, archive, probe, checkAvailability |
| provider | 5 | list, model.list, runtime.{start,send,cancel} |
| session | 10 | list/get/sendMessage/cancel/transcript.get/resume + _suspend/_handoff/_reconcile/_archive |
| task | 6 | create, start, complete, cancel, retrySubtask, nodes |
| workflow | 4 | plan.validate, checkpoint.list, task.cancel, deadletter.resolve |
| knowledge | 3 | search, update, delete |
| kg | 4 | document.list, document.get, citation.get, search |
| gov | 10 | budgetPolicy.{get,update}, budget.{get,reserve,revise}, approvalType.{create,list,get,update}, audit.query |
| approval | 4 | request, list, get, resolve |
| settings | 10 | company.{get,update}, knowledgePolicy.{get,update}, securityPolicy.{get,update}, workspacePolicy.{get,update}, notification.{get,update} |
| audit | 2 | query, intervention.list |
| **合计** | **102** | |

> 其中 **活跃对外方法约 71 个**（见附录 A），其余为内部调用方法或已迁移到 Admin Backend 的管理方法。管理类方法（backend.list/get/create/update 等、knowledge.update/delete、gov.budgetPolicy.* / approvalType.* / audit.query、settings.*、audit.query）已迁移到 Admin Backend REST API（见 3.15 节）。

### 3.15 Admin Backend REST API（管理端点）

> 本节列出 Admin Backend 的 REST API 端点（共 **99 个**）。所有端点均需 JWT 认证（`Authorization: Bearer <token>`），写操作需 `admin` 或 `editor` 角色。

#### 3.15.1 认证（auth）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/auth/login` | POST | 登录（用户名+密码 → JWT） |
| `/api/auth/refresh` | POST | 刷新 token |
| `/api/auth/me` | GET | 获取当前用户信息 |
| `/api/auth/password` | PUT | 修改密码 |

#### 3.15.2 公司与组织（companies）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/companies` | GET | 列出所有公司 |
| `/api/companies/{company_id}` | GET | 获取公司详情 |
| `/api/companies/{company_id}/departments` | GET | 列出部门 |
| `/api/companies/{company_id}/departments/{dept_id}` | GET | 获取部门详情 |
| `/api/companies/{company_id}/employees` | GET | 列出员工 |
| `/api/companies/{company_id}/employees/{emp_id}` | GET | 获取员工详情 |

#### 3.15.3 能力管理（capabilities）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/capabilities` | GET | 列出能力（支持 company_id/status 筛选） |
| `/api/capabilities` | POST | 创建能力 |
| `/api/capabilities/{id}` | GET | 获取能力详情 |
| `/api/capabilities/{id}` | PUT | 更新能力 |
| `/api/capabilities/{id}/submit-review` | POST | 提交审核 |
| `/api/capabilities/{id}/publish` | POST | 发布 |
| `/api/capabilities/{id}/deprecate` | POST | 弃用 |
| `/api/capabilities/{id}/archive` | POST | 归档 |
| `/api/capabilities/{id}/versions` | GET | 列出版本 |
| `/api/capabilities/{id}/versions` | POST | 创建版本 |
| `/api/capabilities/{id}/bindings` | GET | 列出绑定 |
| `/api/capabilities/{id}/bindings` | POST | 创建绑定 |

#### 3.15.4 技能管理（skills）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/skills` | GET | 列出技能 |
| `/api/skills` | POST | 创建技能 |
| `/api/skills/{id}` | GET | 获取技能详情 |
| `/api/skills/{id}` | PUT | 更新技能 |
| `/api/skills/{id}/save-draft` | POST | 保存草稿 |
| `/api/skills/{id}/submit-review` | POST | 提交审核 |
| `/api/skills/{id}/publish` | POST | 发布 |
| `/api/skills/{id}/deprecate` | POST | 弃用 |
| `/api/skills/{id}/archive` | POST | 归档 |
| `/api/skills/{id}/versions` | GET | 列出版本 |
| `/api/skills/{id}/versions` | POST | 创建版本 |

#### 3.15.5 提示词管理（prompts）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/prompts` | GET | 列出提示词 |
| `/api/prompts` | POST | 创建提示词 |
| `/api/prompts/{id}` | GET | 获取提示词详情 |
| `/api/prompts/{id}` | PUT | 更新提示词 |
| `/api/prompts/{id}/save-draft` | POST | 保存草稿 |
| `/api/prompts/{id}/submit-review` | POST | 提交审核 |
| `/api/prompts/{id}/publish` | POST | 发布 |
| `/api/prompts/{id}/deprecate` | POST | 弃用 |
| `/api/prompts/{id}/archive` | POST | 归档 |
| `/api/prompts/{id}/versions` | GET | 列出版本 |
| `/api/prompts/{id}/versions` | POST | 创建版本 |

#### 3.15.6 模板管理（templates）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/templates` | GET | 列出模板 |
| `/api/templates` | POST | 创建模板 |
| `/api/templates/{id}` | GET | 获取模板详情 |
| `/api/templates/{id}` | PUT | 更新模板 |
| `/api/templates/{id}/activate` | POST | 激活模板 |
| `/api/templates/{id}/archive` | POST | 归档模板 |

#### 3.15.7 知识库管理（knowledge）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/knowledge/documents` | GET | 列出文档 |
| `/api/knowledge/documents` | POST | 创建文档 |
| `/api/knowledge/documents/{id}` | GET | 获取文档详情 |
| `/api/knowledge/documents/{id}` | PUT | 更新文档 |
| `/api/knowledge/documents/{id}` | DELETE | 删除文档 |
| `/api/knowledge/documents/{id}/confirm` | POST | 确认文档 |
| `/api/knowledge/documents/{id}/reject` | POST | 驳回文档 |
| `/api/knowledge/reindex` | POST | 重建索引 |
| `/api/knowledge/sources/{source_id}/delete` | POST | 删除来源 |
| `/api/knowledge/ingest/{job_id}/retry` | POST | 重试摄取 |

#### 3.15.8 Provider 管理（providers）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/providers` | GET | 列出 Provider |
| `/api/providers` | POST | 创建 Provider |
| `/api/providers/{id}` | GET | 获取 Provider 详情 |
| `/api/providers/{id}/enable` | POST | 启用 |
| `/api/providers/{id}/disable` | POST | 禁用 |
| `/api/providers/{id}/models` | GET | 列出模型 |
| `/api/providers/{id}/fetch-models` | POST | 拉取模型目录 |
| `/api/providers/{id}/credentials` | PUT | 设置凭证 |
| `/api/providers/{id}/credentials` | DELETE | 删除凭证 |
| `/api/providers/{id}/probe` | POST | 探活 |
| `/api/providers/{id}/pricing` | PUT | 更新价格策略 |
| `/api/providers/{id}/tier-mapping` | PUT | 更新层级映射 |

#### 3.15.9 后端管理（backends）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/backends` | GET | 列出后端 |
| `/api/backends` | POST | 创建后端 |
| `/api/backends/{id}` | PUT | 更新后端 |
| `/api/backends/{id}/enable` | POST | 启用 |
| `/api/backends/{id}/drain` | POST | 排水 |
| `/api/backends/{id}/disable` | POST | 禁用 |
| `/api/backends/{id}/archive` | POST | 归档 |
| `/api/backends/{id}/probe` | POST | 探活 |
| `/api/backends/{id}/set-default` | POST | 设为默认后端 |

#### 3.15.10 治理（governance）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/governance/approval-types` | GET | 列出审批类型 |
| `/api/governance/approval-types` | POST | 创建审批类型 |
| `/api/governance/approval-types/{id}` | PUT | 更新审批类型 |
| `/api/governance/approval-types/{id}` | DELETE | 删除审批类型 |
| `/api/governance/approvals` | GET | 列出审批（桩） |
| `/api/governance/approvals` | POST | 创建审批（桩） |
| `/api/governance/approvals/{id}` | GET | 获取审批详情（桩） |
| `/api/governance/approvals/{id}/resolve` | POST | 决议审批（桩） |
| `/api/governance/budget-policy` | GET | 获取预算策略 |
| `/api/governance/budget-policy` | PUT | 更新预算策略 |
| `/api/governance/budget/{task_id}` | GET | 获取任务预算 |
| `/api/governance/knowledge-rules` | GET | 列出知识规则 |
| `/api/governance/knowledge-rules` | POST | 创建知识规则 |
| `/api/governance/knowledge-rules/{id}` | PUT | 更新知识规则 |
| `/api/governance/knowledge-rules/{id}` | DELETE | 删除知识规则 |

#### 3.15.11 审计（audit）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/audit/logs` | GET | 列出审计日志 |
| `/api/audit/interventions` | GET | 列出人工干预 |

#### 3.15.12 同步（sync）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/sync/config` | GET | 拉取配置数据（供 Sidecar 同步） |

---

## 第 4 章 数据模型

> **Sidecar 数据库**：全部表由 `sidecar/migrations/0001..0062` 顺序生成，共 **88** 张表（含重建中间表如 `tasks_new`、`session_threads_new`、`approvals_new`）。下表为关键表与迁移映射，其余表遵循同名迁移文件。
> **Admin Backend 数据库**：独立数据库 `/tmp/ibreeze_admin.db`，由 Alembic 管理迁移。
> 所有表均带 `company_id` 归属列（除全局表如 `schema_migrations`、`rpc_idempotency`、`local_owner`、`domain_events`、`audit_*` 部分）。迁移脚本一律幂等：`CREATE TABLE IF NOT EXISTS`、`CREATE INDEX IF NOT EXISTS`，`ADD COLUMN` 先 `PRAGMA table_info` 判断。

### 4.1 迁移 → 表映射（核心）

| 迁移 | 主要表 | 说明 |
|------|--------|------|
| 0001 | `schema_migrations` | 迁移版本记录 |
| 0002 | `domain_events` | 领域事件 |
| 0003 | `audit_*`（审计表） | 审计 |
| 0004 | `local_owner` | 本地归属 |
| 0005 | `human_interventions` | 人工干预 |
| 0006 | `rpc_idempotency` | 幂等键预约/回填 |
| 0007 | `backup_*` | 备份表 |
| 0008 | `companies` | 公司（含 `status`、`deleted_at`） |
| 0009 | `departments` | 部门（含 `status`） |
| 0010 | `skills`、`prompts` | 技能/提示词草稿 |
| 0011 | `capabilities` | 能力 |
| 0012 | `capability_snapshots` | 能力快照 |
| 0013 | `capability_metrics` | 能力指标 |
| 0014 | `employee_templates` | 员工模板（绑定 Provider） |
| 0015 | `employees` | 员工（含 `status`、`deleted_at`） |
| 0024 | `employees` +`deleted_at` | 软删列 |
| 0016 | `access_grants` | 授权（含 `revoked_at`） |
| 0027 | `access_grants` +`revoked_at` | |
| 0017 | `employee_drains` | 员工排水 |
| 0044 | `employee_drains_runtime` | 排水运行时 |
| 0018 | `providers`、`backends` | Provider / 后端 |
| 0028 | `backend_registry` | 后端注册 |
| 0030 | `provider_models`（扩展） | 模型 |
| 0031 | `provider_model_prices` | 价格 |
| 0032 | `provider_budget_freezes` | 预算冻结 |
| 0033 | `provider_availability` | 可用性 |
| 0034 | `credential_runtime` | 凭证运行时 |
| 0019 | `sessions`、`session_threads` | 会话（0040 扩展；`session_threads_new` 重建） |
| 0041 | `conversation_events` | 会话事件 |
| 0042 | `session_transfer_journal` | 转交日志 |
| 0043 | `session_watermarks` | 会话水位 |
| 0020 | `knowledge` | 知识库 |
| 0045 | `knowledge_sources` | 来源 |
| 0046 | `knowledge_citations_jobs` | citation 作业 |
| 0047 | `index_generations` | 索引生成 |
| 0048 | `knowledge_governance_audit` | 知识治理审计 |
| 0049 | `knowledge_documents`（扩展） | 文档 |
| 0050 | `knowledge_fts` / 触发器 | 全文索引 |
| 0021 | `tasks`（0057 扩展；`tasks_new` 重建） | 任务 |
| 0051 | `task_nodes`（扩展） | 子任务节点 |
| 0052 | `task_assignments`（扩展） | 任务分配 |
| 0053 | `plan_generations` | plan 生成 |
| 0054 | `checkpoints` | 检查点 |
| 0056 | `dead_letters` | 死信 |
| 0058 | `evidence_*` | 证据表 |
| 0022 | `governance` | 治理基础 |
| 0035 | `governance_approval` | 治理审批 |
| 0036 | `governance_*` | 治理表 |
| 0037 | `governance_budget_*` | 预算扩展 |
| 0038 | `settings_policies` | 设置策略 |
| 0059 | `employee_reporting_closure` | 员工汇报闭包 |
| 0060 | `dissolution_watermarks` | 解散水位（6 消费者） |
| 0061 | `backend_health_probe` | 后端探活 |
| 0062 | `backend_provider_link` | 后端-Provider 关联 |

### 4.2 核心实体列（节选，必须与迁移核对）

- **companies**：`id, name, status(initializing/active/dissolving/dissolved/deleted), deleted_at, created_at, updated_at`
- **departments**：`id, company_id, parent_id, name, leader_id, status(active/frozen/archived), created_at`
- **employees**：`id, company_id, department_id, template_id, manager_id, status(created/active/suspended/archived/deleted), deleted_at`
- **employee_templates**：`id, company_id, provider_id, name, archived, ...`
- **access_grants**：`id, company_id, subject, object, revoked_at, ...`
- **providers / backends**：`id, company_id, status, ...`
- **sessions**：`id, company_id, employee_id, template_id, status(active/running/waiting_backend/waiting_approval/dormant/archived/failed/recovering), readonly_flag`
- **tasks / task_nodes**：`id, company_id, session_id, status, parent_node_id, ...`
- **capabilities / skills / prompts**：`id, company_id, status(draft/review/published/deprecated/archived), current_version, ...`
- **rpc_idempotency**：`idempotency_key, method, params, status, result, error, created_at`
- **dissolution_watermarks**：`company_id, stage, ...`（驱动 `dissolving → employee_drain → dissolved`）

### 4.3 重建表规则

迁移中对 `tasks`、`session_threads`、`approvals` 采用"建 `_new` → 拷贝 → 改名"的重建模式；重跑迁移脚本安全幂等。

### 4.4 Admin Backend 数据模型

Admin Backend 使用独立的 SQLAlchemy ORM 模型，通过 Alembic 管理迁移。主要模型：

- **AdminUser**：`user_id, username, password_hash, role(admin/editor/viewer), created_at, updated_at`
- **Capability**：`capability_id, company_id, name, description, status, current_version, version, created_at, updated_at`
- **Skill**：`skill_id, company_id, name, description, prompt_asset_id, status, version, created_at, updated_at`
- **PromptAsset**：`prompt_id, company_id, name, description, content, status, version, created_at, updated_at`
- **EmployeeTemplate**：`template_id, company_id, name, role, description, provider_id, capability_id, model, status, version, created_at, updated_at`
- **KnowledgeDocument**：`knowledge_id, company_id, title, content, source_type, source_id, category, status, governance_confirmed, created_at, updated_at`
- **Provider**：`provider_id, company_id, name, provider_type, config, status, version, created_at, updated_at`
- **Backend**：`backend_id, company_id, name, backend_type, provider_id, workspace_root, status, concurrency, version, created_at, updated_at`
- **ApprovalType**：`type_id, company_id, name, description, config, version, created_at, updated_at`
- **AuditLog**：`log_id, company_id, audit_type, actor_id, action, resource_type, resource_id, details, trace_id, created_at`
- **BudgetPolicy**：`id, company_id, version, config, status, created_at`
- **KnowledgeRule**：`rule_id, company_id, name, description, category, action, enabled, config, created_at, updated_at`

---

## 第 5 章 状态机总览

> 下列状态转移常量来自对应 `models.py` / `service.py`，是后端强制约束。

### 5.1 company

`initializing → active → dissolving → dissolved`，外加软删 `deleted`（通过 `deleted_at` 标记）。`dissolve` 触发 `dissolution_watermarks` 6 个消费者流水线，完成 `employee_drain` 后落 `dissolved`。

### 5.2 department

`active ↔ frozen`，`active/frozen → archived`。冻结态禁止在其下创建子部门。

### 5.3 employee

`created → active → suspended → archived → deleted(软)`；`suspended → active(resume)`。`deleted` 为 `deleted_at` 软删。

### 5.4 task / task_node / workflow run

- **TASK_TRANSITIONS**：含 `cancelling` 等；`create → start → complete`，`→ cancelling → cancelled`，失败可 `retrySubtask`。
- **NODE_TRANSITIONS**：子任务节点状态转移（与 task 联动）。
- **RUN_TRANSITIONS**：工作流运行态。
- **GENERATION_TRANSITIONS**：plan / 生成态（见 `task/models.py`）。

### 5.5 backend

`enabled → draining → disabled → archived`；`draining` 期内只收尾不接新。

### 5.6 capability / skill / prompt

统一：`draft → review → published → deprecated → archived`；`saveDraft` 留在 `draft`；`submitReview` 进 `review`；`publish` 进 `published`。

### 5.7 session

`active → running → waiting_backend / waiting_approval → dormant → archived`，异常 `failed → recovering → active`。当员工 `suspended` 或模板 `archived` 时会话置 `READONLY`。

### 5.8 grant

`active → expired / revoked`（`revoked_at` 标记撤销）。

### 5.9 company dissolution 流水线

`dissolving` 经 `dissolution_watermarks` 6 个消费者顺序处理（含 `employee_drain`），全部完成后转 `dissolved`。

---

## 第 6 章 端到端业务规则

> 以下不变量摘自 `docs/功能链路测试用例.md`，是系统强制行为，重建时必须保证。

1. **跨公司拒绝**：任何以 `company_id` 过滤的查询/写操作，跨公司访问一律拒绝（返回权限错误）。
2. **预算拦截**：任务创建/预算预留超出 `gov.budgetPolicy` 时，触发 `gov.approval` 审批或拒绝（预算修订需 `budget_revision_lock`）。
3. **冻结部门禁建子部门**：父部门 `frozen` 时 `org.department.create` 拒绝。
4. **含员工部门禁删**：部门下仍有员工或子部门时 `org.department.delete` 拒绝。
5. **会话 READONLY 联动**：会话所属员工 `suspended` 或所用模板 `archived` → 会话进入只读，禁止 `sendMessage` 改动。
6. **deadletter 联动 task**：工作流节点失败进入 `dead_letters`，经 `workflow.deadletter.resolve` 处理后回写 task 状态。
7. **dissolve → employee_drain**：`org.company.dissolve` 触发 `dissolution_watermarks`，先完成员工排水再 `dissolved`，期间禁止新写。
8. **权限引擎统一解析**：`org.permission.resolve` 委托 `cap.engine.resolve`，所有能力/资源访问经同一引擎裁决。
9. **幂等回填**：写方法带 `idempotency_key` 重复提交，返回首次结果不重复生效（见 1.5 / 3.4 约定）。
10. **审批联动**：plan 校验（`workflow.plan.validate`）、预算修订等需要人工决策时创建 `approval`，`approval.resolve` 决议后才继续。

---

## 第 7 章 构建与部署

### 7.1 Sidecar 后端运行

- SQLite 数据库：`/tmp/acos.db`（aiosqlite 异步访问）。
- LanceDB：sidecar 内嵌，用于知识库/文档/向量索引。
- UDS 监听：`/tmp/acos.sock`。
- 启动顺序：应用（Tauri/Rust）拉起 sidecar 进程 → sidecar 执行 `migrations` 全部迁移 → 监听 UDS → 注册全部活跃方法 → 健康检查 `sys.health` 通过。

### 7.2 Admin Backend 运行

```bash
# 在 admin-backend 下
cd admin-backend
pip install -r requirements.txt
python -m app.main
```

- 默认监听：`http://127.0.0.1:50080`
- SQLite 数据库：`/tmp/ibreeze_admin.db`
- 迁移管理：Alembic（`admin-backend/migrations/`）
- 环境变量前缀：`IBREEZE_ADMIN_`（如 `IBREEZE_ADMIN_PORT`）
- 首次启动自动创建管理员账号（admin/admin123）

### 7.3 Admin Frontend 运行

```bash
# 在 admin-frontend 下
cd admin-frontend
npm install
npm run dev
```

- 默认监听：`http://localhost:50081`
- 配置后端地址：`.env` 文件中 `VITE_API_BASE_URL=http://127.0.0.1:50080`

### 7.4 迁移执行

**Sidecar 迁移**：
- 所有表结构变更**只**通过 `sidecar/migrations/YYYYMMDDHHMMSS_description.sql`。
- 命名格式 `YYYYMMDDHHMMSS_description.sql`（现有为 `0001..0062` 顺序编号）。
- 脚本幂等：`CREATE TABLE IF NOT EXISTS`、`CREATE INDEX IF NOT EXISTS`；`ADD COLUMN` 先 `PRAGMA table_info` 判断；`DROP` 旧表放最后且确认新表就绪后。
- 迁移失败修复脚本后可直接重跑。

**Admin Backend 迁移**：
- 使用 Alembic 管理数据库迁移。
- 迁移文件位于 `admin-backend/migrations/versions/`。
- 命令：`alembic upgrade head`（应用迁移）/ `alembic downgrade -1`（回滚）。

### 7.5 桌面端构建（Tauri 2）

```bash
# 在 apps/desktop 下
npm install
npm run tauri build
```

产物位于：

```
apps/desktop/src-tauri/target/release/bundle/
```

- `app` 标识：`com.ibreeze.desktop`
- `productName`：`iBreeze`

### 7.6 前端调用约定回顾

- **Sidecar RPC**：所有 RPC 经 `invoke('sys_rpc_call', { method, params: JSON.stringify(params) })`。
- **Admin Backend REST**：所有管理操作经 `http://127.0.0.1:50080/api/*`，需 JWT 认证。
- 写方法必须带 `idempotency_key`（Sidecar RPC）；前端时间统一东八区；数值最多 2 位小数不补零。

### 7.7 关闭与运维

- **Sidecar 优雅关闭**：上层调用 `sys.shutdown` → sidecar 进入关闭流程（见 `supervisor.py`：`sys.shutdown → SIGTERM → SIGKILL`）。
- **Admin Backend 优雅关闭**：Ctrl+C 或 `kill` 信号 → FastAPI lifespan 处理关闭。
- **健康探测**：上层轮询 `sys.health`（Sidecar）或 `/health`（Admin Backend）判断是否存活。

---

## 附录 A：方法清单完整索引

> 按域快速索引（便于核对），完整定义见第 3 章。

### Sidecar 活跃方法（~71 个对外方法）

- **echo (8)**：string, int, float, bool, array, object, null, mirror
- **sys (4)**：health, shutdown, migration.status, sync.trigger
- **org (35)**：company.{list,get,create,update,delete,restore,activate,dissolve}; department.{list,get,create,update,delete,move,setLeader,freeze,unfreeze,archive}; employee.{list,get,create,update,delete,activate,suspend,resume,archive,transfer,setManager}; grant.{create,get,list,revoke}; permission.resolve; graph.get
- **cap (2)**：snapshot.build, engine.resolve
- **backend (2)**：checkAvailability, probe
- **provider (5)**：list, model.list, runtime.{start,send,cancel}
- **session (10)**：list, get, sendMessage, cancel, resume, transcript.get, _suspend, _handoff, _reconcile, _archive
- **task (6)**：create, start, complete, cancel, retrySubtask, nodes
- **workflow (4)**：plan.validate, checkpoint.list, deadletter.resolve, task.cancel
- **knowledge (1)**：search
- **kg (4)**：document.{list,get}, citation.get, search
- **gov (3)**：budget.{get,reserve,revise}
- **approval (4)**：request, list, get, resolve

### 已迁移到 Admin Backend 的 Sidecar 方法

以下方法在 Sidecar 中仍注册但已不作为对外活跃方法，管理操作统一迁移到 Admin Backend REST API：

- **backend (8)**：list, get, create, update, setDefault, enable, drain, archive
- **provider (1)**：list（管理列表）
- **knowledge (2)**：update, delete
- **gov (7)**：budgetPolicy.{get,update}, approvalType.{create,list,get,update}, audit.query
- **settings (10)**：company.{get,update}, knowledgePolicy.{get,update}, securityPolicy.{get,update}, workspacePolicy.{get,update}, notification.{get,update}
- **audit (1)**：query
- **org.template (6)**：list, get, create, update, activate, archive

### Admin Backend REST 端点（99 个）

- **auth (4)**：login, refresh, me, password
- **companies (6)**：list, get, list_departments, get_department, list_employees, get_employee
- **capabilities (12)**：list, create, get, update, submit_review, publish, deprecate, archive, list_versions, create_version, list_bindings, create_binding
- **skills (11)**：list, create, get, update, save_draft, submit_review, publish, deprecate, archive, list_versions, create_version
- **prompts (11)**：list, create, get, update, save_draft, submit_review, publish, deprecate, archive, list_versions, create_version
- **templates (6)**：list, create, get, update, activate, archive
- **knowledge (10)**：list_documents, get_document, create_document, update_document, delete_document, confirm_document, reject_document, reindex, delete_source, retry_ingest
- **providers (12)**：list, create, get, enable, disable, list_models, fetch_models, set_credentials, delete_credentials, probe, update_pricing, update_tier_mapping
- **backends (9)**：list, create, update, enable, drain, disable, archive, probe, set_default
- **governance (15)**：list_approval_types, create_approval_type, update_approval_type, delete_approval_type, list_approvals, get_approval, resolve_approval, create_approval, get_budget_policy, update_budget_policy, get_task_budget, list_knowledge_rules, create_knowledge_rule, update_knowledge_rule, delete_knowledge_rule
- **audit (2)**：list_audit_logs, list_interventions
- **sync (1)**：sync_config

> 说明：Sidecar 中的 `session`、`knowledge` 等域的部分方法在代码中注册但实际由内部调用，文档以代码中 `register_method` / `_reg` 实际注册数为准。Admin Backend 端点数量以 FastAPI 路由注册为准。
