# =============================================================================
# iBreeze Docker Compose — 开发环境端口暴露覆盖
# =============================================================================
# 使用方式：
#   docker compose -f deploy/docker-compose.yml -f deploy/docker-compose.dev.yml up -d
# =============================================================================

services:
  postgres:
    ports:
      - "51543:5432"

  minio:
    ports:
      - "51900:9000"
      - "51901:9001"

  backend-api:
    ports:
      - "51080:51080"

  admin-web:
    ports:
      - "51421:80"

  nginx:
    ports:
      - "8080:80"
