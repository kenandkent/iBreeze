{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ibreeze.ai/schemas/rpc-meta.v1",
  "title": "RPC Meta",
  "description": "Common meta fields for all RPC requests",
  "type": "object",
  "properties": {
    "trace_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique trace identifier for request tracing"
    },
    "ipc_session_id": {
      "type": "string",
      "format": "uuid",
      "description": "IPC session identifier, null for pre-handshake requests"
    },
    "window_session_id": {
      "type": "string",
      "format": "uuid",
      "description": "Window session identifier"
    },
    "idempotency_key": {
      "type": "string",
      "format": "uuid",
      "description": "Idempotency key for write methods; null for read methods"
    },
    "deadline_at": {
      "type": "string",
      "format": "date-time",
      "description": "Absolute request deadline used by the IPC multiplexer"
    }
  },
  "required": ["trace_id"],
  "additionalProperties": false
}
